---
import { createAvatar } from "@dicebear/core";
import { bottts } from "@dicebear/collection";
interface Props {
  seed: string;
}
const { seed } = Astro.props;

const avatar = createAvatar(bottts, {
  seed,
});
---

<Fragment set:html={avatar.toString()} />
